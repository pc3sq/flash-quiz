<%= form_for(@flash_card) do |f| %>
  <% if @flash_card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@flash_card.errors.count, "error") %> prohibited this flash_card from being saved:</h2>

      <ul>
      <% @flash_card.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_area :title %>
  </div>
  <div class="field">
    <%= f.label :question %><br>
    <%= f.text_area :question %>
  </div>
  <div class="field">
    <%= f.label :answer %><br>
    <%= f.text_area :answer %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
