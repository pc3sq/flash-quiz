<h1 id="topheading">Welcome To Flash Quiz!</h1>

<!-- <table>
  <thead>
    <tr>
      <th>Score</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @quizzes.each do |quiz| %>
      <tr>
        <td><%= quiz.score %></td>
        <td><%= link_to 'Show', quiz %></td>
        <td><%= link_to 'Edit', edit_quiz_path(quiz) %></td>
        <td><%= link_to 'Destroy', quiz, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table> -->

<br>
<section id="quiz">
  <button id="startquiz">Start A Quiz?</button>
</section>

<br>
<br>

<section id="flash-cards-menu">
  <h3>Flash Cards</h3>
  <%= link_to 'See Flash Cards', flash_cards_path %>
  <br>
  <%= link_to 'Add Flash Cards', new_flash_card_path %>
</section>
